import React from "react";
import { Divider, Toolbar, Typography } from "@material-ui/core";

const Header = () => {
  return (
    <>
      <Toolbar>
        <Typography variant="h6">Student Details Forms</Typography>
      </Toolbar>
      <Divider />
    </>
  );
};

export default Header;
